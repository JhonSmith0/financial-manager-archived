import styled from "styled-components"

export const Input = styled.input`
    font-size: 1.4rem;
    padding: 0.6rem;

    border: solid 1px #999;
    outline: none;
    width: max-content;

    &:focus-visible {
        background: #f1f3f5;
    }
`
